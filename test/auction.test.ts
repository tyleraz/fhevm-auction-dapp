describe("ConfidentialAuction", () => {
  it("should handle encrypted bids correctly", async () => {
    // deploy, create FHE client...
    // submit bids 10, 20, 15
    // call revealWinner
    // decrypt and assert that highest bid == 20
  });
});
